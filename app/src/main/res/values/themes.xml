<resources>
    <!-- AppCompat parent (not Material3 XML) — Compose draws everything; the
         Activity must be AppCompat for per-app locales on minSdk 31. -->
    <style name="Theme.LangTutor" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">#FFF6EC</item>
    </style>
</resources>
